name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r backend/requirements.txt
          pip install -r requirements.txt

      - name: Unit tests
        working-directory: backend
        run: |
          python -m pytest ../tests -v --tb=short

      - name: Syntax check
        working-directory: backend
        run: python -m py_compile main.py ingest.py

      - name: Smoke test (offline steps)
        working-directory: backend
        env:
          GROQ_API_KEY_1: ${{ secrets.GROQ_API_KEY_1 }}
        run: |
          python ../smoke_test.py 2>&1 | tee smoke_out.txt
          if grep -q "SUMMARY: 5 passed" smoke_out.txt; then
            echo "Smoke test: all 5 steps passed"
          elif [ -z "$GROQ_API_KEY_1" ] && grep -q "GROQ_API_KEY_1 not found" smoke_out.txt; then
            echo "Smoke test: skipped (GROQ_API_KEY_1 secret not configured — add it in repo Settings > Secrets)"
          else
            exit 1
          fi
